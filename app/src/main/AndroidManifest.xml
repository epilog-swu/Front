<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.DayNight.NoActionBar"
        tools:targetApi="31">
        <activity android:name=".signUp1Activity"/>

        <activity android:name=".signUp2Activity"/>
        <activity android:name=".signUp3Activity"
            android:exported="true"
            android:label="@string/signup"
            android:theme="@style/Theme.Epilog">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
        </activity>
        <activity android:name=".signUp4Activity"/>

        <activity android:name=".signUp5Activity"/>
        <activity android:name=".DiaryWriteActivity"/>


        <activity android:name=".DiaryEditActivity"/>
      <activity android:name=".startActivity"/>
        <activity
            android:name=".MainActivity"/>
        <activity android:name=".ActivityShowSuccessDialog"
            android:theme="@style/TransparentTheme" />
        <activity android:name=".ActivityShowFailDialog"
            android:theme="@style/TransparentTheme" />
    </application>

</manifest>